<!--pages/shadow-detail/shadow-detail.wxml-->
<view class="container">
    <view class="base-info">
        <base-info-card icon="{{curShadow.icon}}" name="{{curShadow.name}}" property="{{curShadow.property}}" divinatory="{{curShadow.divinatory}}" tags="{{['待定']}}"></base-info-card>
        <view class="effect">
            <view class="second-title">残影效果</view>
            <text>{{curShadow.effect}}</text>
        </view>
    </view>
</view>
