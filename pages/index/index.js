// index.js

Page({
	data: {
		// test: []
	},
	onLoad: function () {
		// let data = require('../../data/shadows').shadows.data
		
		// data.map((item,index) => {
		// 	item.idx = index
		// 	item.icon = "/image/shadow/"+ item.id +".png"
		// })
		// this.setData({
		// 	test:data
		// })
	}
})